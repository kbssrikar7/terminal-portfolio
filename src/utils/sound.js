// Sound system completely disabled per user request
export const playSound = (type) => {
    // No-op
};

export const toggleBGM = (play) => {
    // No-op
};

export const toggleMute = () => {
    return true;
};
